# Recall

Use this order:

1. Read `.fcc/context/handoff.md` for current state.
2. Read `CLAUDE.local.md` for local-machine facts.
3. Use MemSearch for persistent cross-session memory when it is installed.
4. Use Token Savior for code retrieval.
5. Query FCC SQLite sidecar handles for raw terminal/tool output.

Do not replay old chat context into the prompt.
