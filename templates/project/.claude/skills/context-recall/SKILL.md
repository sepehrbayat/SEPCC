---
name: context-recall
description: Recall project state without prompt replay. Use when resuming work, checking prior decisions, or needing context from earlier sessions.
---

# Context Recall

1. Read `.fcc/context/handoff.md`.
2. Read `CLAUDE.local.md` only for local-machine facts needed now.
3. Use MemSearch for persistent memory when it is installed.
4. Use Token Savior for code retrieval by symbol or dependency.
5. Query the FCC SQLite sidecar for raw output handles when exact logs matter.

Return only the facts needed for the current task.
